{
  "name": "the-brass-guardian-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:archive": "python3 scripts/generate-archive-assets.py",
    "dev": "astro dev",
    "prebuild": "npm run verify:publication",
    "build": "astro build",
    "build:archive-preview": "PUBLICATION_PREVIEW=1 astro build --outDir dist-preview",
    "preview": "astro preview",
    "test": "node --test scripts/*.test.mjs",
    "verify:publication": "node scripts/verify-publication-boundary.mjs",
    "verify": "npm run test && npm run build && node scripts/verify-site.mjs",
    "verify:archive-preview": "npm run test && npm run build:archive-preview && node scripts/verify-archive-preview.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "2.0.1",
    "@vercel/speed-insights": "2.0.0",
    "astro": "7.2.0"
  }
}
